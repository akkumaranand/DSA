#include <bits/stdc++.h>
using namespace std;

// Iterator
int main()
{
   unordered_set<string> s;
   int n;
   cin>>n;
   for(int i = 0; i< n; i++){
    cout<<"Enter the string "<<endl;
    string str;
    cin>>str;
    s.insert(str);
   }

   int q ;
   cin>>q;
   while(q--){
    string str;
    cout<<"Enter the string to check it is present or not "<<endl;
    cin>>str;
    if(s.find(str) == s.end()){
        cout<<"NO"<<endl;
    }
    else{
        cout<<"Yes"<<endl;
    }
    
   }

    return 0;
}
